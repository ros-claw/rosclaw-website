import type { Metadata } from "next";
import Link from "next/link";
import { ArrowRight, CheckCircle2, CircleSlash2, LockKeyhole, ShieldCheck, UserCheck } from "lucide-react";
import { Footer } from "@/components/footer";
import { StatusBadge } from "@/components/status/status-badge";

export const metadata: Metadata = { title: "Safety & Authority | ROSClaw", description: "Understand ROSClaw's physical authority boundary, approval gates, execution modes, and evidence model.", alternates: { canonical: "/safety" } };

const layers = [
  ["01", "Agent proposal", "Models and workers may reason, plan, and request tools. A proposal is not authority.", "cognitive"],
  ["02", "Policy + approval", "Scopes, limits, mode, and operator approval are evaluated before physical dispatch.", "physical"],
  ["03", "rosclawd authority", "The daemon mediates the physical lane and rejects unapproved or unavailable actions.", "physical"],
  ["04", "Executor boundary", "A body-specific executor applies limits and performs only the bounded action.", "physical"],
  ["05", "Verification + receipt", "Outcome evidence and an auditable receipt return to the mission.", "verified"],
] as const;

export default function SafetyPage() { return <main className="min-h-screen bg-[#060809] pt-16"><section className="runtime-grid border-b border-white/[0.08] px-4 py-20 sm:px-6 md:py-28 lg:px-8"><div className="mx-auto max-w-[1440px]"><div className="flex flex-wrap gap-3"><StatusBadge status="Component Tested"/><StatusBadge status="Simulation Verified"/></div><p className="section-kicker mt-8">Safety & authority</p><h1 className="mt-5 max-w-5xl text-balance text-4xl font-semibold tracking-[-0.05em] text-white sm:text-6xl">No model output directly controls a robot.</h1><p className="mt-6 max-w-3xl text-lg leading-relaxed text-white/54">ROSClaw makes the physical boundary explicit: Agents propose; policy and operators approve; rosclawd authorizes; bounded executors act; evidence verifies.</p></div></section><section className="px-4 py-20 sm:px-6 md:py-28 lg:px-8"><div className="mx-auto max-w-[1440px]"><p className="section-kicker">Authority chain</p><ol className="mt-8 grid border border-white/10 bg-[#070a0b] lg:grid-cols-5">{layers.map(([step,title,body,lane],index)=><li key={title} className={`relative min-h-56 p-6 ${index<4?"border-b border-white/10 lg:border-b-0 lg:border-r":""}`}><span className={`font-mono text-[10px] ${lane==="physical"?"text-physical-orange":lane==="verified"?"text-emerald-300":"text-cognitive-cyan"}`}>{step}</span><h2 className="mt-10 text-lg font-semibold text-white">{title}</h2><p className="mt-3 text-sm leading-relaxed text-white/42">{body}</p></li>)}</ol><div className="mt-12 grid gap-px border border-white/10 bg-white/10 md:grid-cols-3">{[[LockKeyhole,"Default-deny scopes","Hardware access requires declared capability and mode."],[UserCheck,"Human approval gates","High-consequence steps stop for explicit review."],[CheckCircle2,"Evidence, not claims","Receipts record the bounded request, result, and verification."]].map(([Icon,title,body])=>{const C=Icon as typeof ShieldCheck;return <article key={String(title)} className="bg-[#080b0c] p-7"><C className="h-5 w-5 text-physical-orange"/><h3 className="mt-7 text-lg font-semibold text-white">{String(title)}</h3><p className="mt-3 text-sm leading-relaxed text-white/45">{String(body)}</p></article>})}</div></div></section><section className="border-y border-white/[0.08] bg-[#050708] px-4 py-20 sm:px-6 lg:px-8"><div className="mx-auto grid max-w-[1440px] gap-10 lg:grid-cols-2"><div><CircleSlash2 className="h-6 w-6 text-physical-orange"/><h2 className="mt-6 text-3xl font-semibold text-white">What the current release does not claim</h2></div><ul className="space-y-4 text-sm leading-relaxed text-white/52">{["A Hub listing does not mean safe or verified physical execution.","Component-tested hardware support is not an independently verified end-to-end robot run.","Main Experimental capabilities are not promoted to Stable by the installer.","An LLM response, worker result, or MCP tool call cannot override rosclawd policy."].map(item=><li key={item} className="flex gap-3 border-b border-white/[0.08] pb-4"><span className="text-physical-orange">—</span>{item}</li>)}</ul></div></section><section className="px-4 py-20 text-center sm:px-6"><h2 className="text-3xl font-semibold text-white">Start in simulation. Inspect the evidence.</h2><Link href="/start?path=simulation" className="focus-ring mt-7 inline-flex items-center gap-2 bg-cognitive-cyan px-6 py-3 font-semibold text-[#021012]">Run verified path <ArrowRight className="h-4 w-4"/></Link></section><Footer/></main> }
